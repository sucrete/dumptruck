import Dump from "../../components/Dump";
console.log("the modulus of 0 = " + (0 % 0));
export default function gBaWhnm1kS() {
  return (
    <div>
      <Dump id="Rob">
        {new Array(15).fill(0).map((url, index) => {
          return index % 2 == 0 ? (
            <img
              className="workComponent"
              src="/gBaWhnm1kS--0.png"
              key={index}
            />
          ) : (
            <img
              className="workComponent"
              src="/gBaWhnm1kS--1.gif"
              key={index}
            />
          );
        })}
      </Dump>
      <style jsx>{`
        .workComponent {
          max-width: 425px;
        }
      `}</style>
    </div>
  );
}
